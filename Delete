using System;

namespace Delete
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("To delete objects type ID, if no object is to be deleted type 0 to return to main: ");
            int IDtoDelete = int.Parse(Console.ReadLine());

            if (IDtoDelete == 0)
            {
                Console.WriteLine("Nothing will be deleted, returning to main");
                //kommando för att återgå till meny?
            }
            else
            {
                Console.WriteLine("answer + " is to be deleted");
                var IDdelete = JSONFIL.FirstOrDefault(obj => obj["ID"].Value<int>() == IDtoDelete);

                JSONFIL.Remove(IDdelet);
                
                //bekräfta delete? måste ha searchfunktion som returnerar IDtoDelete true or false
                // Kommando för att återgå till början?
            }
        }
    }
